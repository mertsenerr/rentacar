{
  "_comment": "Copy this file to appsettings.Development.json and fill in real values. The real file is gitignored.",

  "MongoDbSettings": {
    "_comment": "Local Mongo: mongodb://localhost:27017  |  Atlas: mongodb+srv://USER:PASS@CLUSTER/?retryWrites=true&w=majority",
    "ConnectionString": "mongodb://localhost:27017",
    "DatabaseName": "rentacar_dev"
  },
  "JwtSettings": {
    "_comment": "Generate with: $b=New-Object byte[] 48; [System.Security.Cryptography.RandomNumberGenerator]::Create().GetBytes($b); [Convert]::ToBase64String($b)",
    "Secret": "REPLACE_WITH_RANDOM_BASE64_STRING_AT_LEAST_32_CHARS",
    "Issuer": "CorporateElite.API",
    "Audience": "CorporateElite.Client",
    "ExpirationInMinutes": 60
  },
  "Resend": {
    "_comment": "Leave ApiToken empty if you're not testing email locally.",
    "ApiToken": "",
    "FromEmail": "onboarding@resend.dev",
    "FromName": "Moviqar (dev)"
  },
  "Frontend": {
    "BaseUrl": "http://localhost:4200"
  },
  "Cors": {
    "AllowedOrigins": [
      "http://localhost:4200",
      "http://127.0.0.1:4200"
    ],
    "AllowedOriginPatterns": []
  }
}
